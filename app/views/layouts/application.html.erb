<!DOCTYPE html>
<html lang="en" data-theme="zcr">
  <head>
    <title><%= content_for(:title) || "Zero Config Rails Tools - Free Rails Developer Tools" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-prefetch" content="false">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#1e293b">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <% if seo_page_indexable? %>
      <link rel="canonical" href="<%= seo_canonical_url %>">
      <%= seo_markdown_alternate_tag %>
      <%= seo_json_ld %>
    <% end %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/favicon-32x32.png" type="image/png" sizes="32x32">
    <link rel="icon" href="/favicon-16x16.png" type="image/png" sizes="16x16">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <meta name="keywords" content="rails generator, rails new generator, gem installer, boring generators, rails developer tools, zen gem, zero config rails, zero config rails tools, zero config rails generators">
    <meta name="title" content="Zero Config Rails Tools - Free Rails Developer Tools">
    <meta name="description" content="<%= SeoDiscoverability::Site::DESCRIPTION %>">

    <meta name="twitter:title" content="Zero Config Rails Tools - Free Rails Developer Tools">
    <meta name="twitter:description" content="<%= SeoDiscoverability::Site::DESCRIPTION %>">
    <meta name="twitter:image" content="<%= image_url('/meta/cover.png') %>">
    <meta name="twitter:card" content="summary_large_image">

    <meta property="og:title" content="Zero Config Rails Tools - Free Rails Developer Tools">
    <meta property="og:url" content="<%= seo_canonical_url %>">
    <meta property="og:image" content="<%= image_url('/meta/cover.png') %>">
    <meta property="og:description" content="<%= SeoDiscoverability::Site::DESCRIPTION %>">
  </head>

  <body class="min-h-screen bg-base-200 text-base-content flex flex-col antialiased">
    <%= render partial: "shared/navbar" %>

    <main class="flex-grow">
      <%= yield %>
    </main>

    <%= render partial: "shared/footer" %>
    <%= seo_markdown_pointer %>
  </body>
</html>
