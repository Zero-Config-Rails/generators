# == Schema Information
#
# Table name: configurations
#
#  id                :bigint           not null, primary key
#  configuration_key :string
#  description       :string
#  fieldable_type    :string
#  is_active         :boolean          default(TRUE)
#  is_required       :boolean          default(FALSE)
#  label             :string
#  created_at        :datetime         not null
#  updated_at        :datetime         not null
#  fieldable_id      :bigint
#  generator_id      :bigint           not null
#

devise_model:
  generator: devise
  configuration_key: model_name
  label: Model name
  description: Model used for authentication
  fieldable: devise_model (TextField)
  is_required: true

annotate_skip:
  generator: annotate
  configuration_key: skip_on_db_migrate
  label: Skip on migrate
  description: Skip annotating after db:migrate
  fieldable: annotate_skip (BooleanField)
  is_required: false
